{
  "private": true,
  "scripts": {
    "dev": "parcel --dist-dir public src/mux.html",
    "build": "parcel build src/mux.html --dist-dir public"
  },
  "dependencies": {
    "@mux/spaces-web": "^1.1.1",
    "@vercel/edge": "^0.1.2"
  },
  "devDependencies": {
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "typescript": "4.9.5"
  }
}
